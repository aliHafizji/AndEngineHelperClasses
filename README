AndEngine Helper classes

This repo is a collection of 3rd party helper classes to help make the lives of game devs a little easy.

Helper classes
	- Slider - Little slider control to allow the user to set the music/sfx level.
	- LevelSelector - A simple level selector grid, which allows the user to scroll horizontally and vertically.

